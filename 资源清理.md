# 资源清理

## 1. 分析对象

本次分析基于当前已有的 macOS ARM64 构建产物：

```text
out/格物AI-0.2.18-mac-arm64.dmg
out/格物AI-0.2.18-mac-arm64.zip
out/mac-arm64/格物AI.app
```

实测体积：

| 产物 | 体积 |
| --- | ---: |
| DMG | 373 MB |
| ZIP | 374 MB |
| 解包后的应用 | 890 MB |
| `out/` 整体 | 2.4 GB |

`out/` 整体的 2.4 GB 包含多个版本的 DMG、ZIP 和解包目录，不代表单个安装包大小。发布包当前实际约为 373 MB。

## 2. 应用体积构成

解包后的 `.app` 主要由以下两部分组成：

| 目录 | 体积 | 说明 |
| --- | ---: | --- |
| `Contents/Resources` | 675 MB | 应用代码、Node 依赖和内置运行时 |
| `Contents/Frameworks` | 215 MB | Electron Framework 及辅助进程 |

其中 Electron Framework 约 213 MB，属于 Electron/Chromium 的基础体积，短期内不适合作为主要清理目标。

### 2.1 Resources 构成

| 内容 | 体积 | 说明 |
| --- | ---: | --- |
| `app.asar` | 396 MB | 打包后的业务代码及 Node 依赖 |
| `app.asar.unpacked` | 177 MB | 原生模块和必须解包的资源 |
| Node.js Runtime | 48 MB | `node-darwin-arm64.tar.gz` |
| `big-paste-test.png` | 26 MB | 未发现业务引用的测试图片 |
| mcporter | 13 MB | MCP CLI 运行时 |
| Sudocode | 8.8 MB | `v0.1.16-scode-macos-arm64.tar.gz` |
| Nexus | 5.4 MB | `v0.7.2-nexusd-cluster-macos-aarch64.tar.gz` |

Node.js、Sudocode 和 Nexus 三项合计约 62 MB，不是安装包超过 300 MB 的主要原因。主要占用来自 Electron 和 `app.asar` 内的生产依赖。

## 3. 主要可清理项

### 3.1 删除未引用的 `big-paste-test.png`

文件：

```text
public/big-paste-test.png
```

该文件约 26 MB，当前未发现代码引用。由于 `public` 同时进入 `app.asar`，又通过 `extraResources` 复制到应用 Resources，该文件在安装后应用中出现两次：

```text
app.asar/public/big-paste-test.png
Contents/Resources/big-paste-test.png
```

预期收益：

| 指标 | 预计减少 |
| --- | ---: |
| 安装后应用 | 约 52 MB |
| DMG/ZIP | 约 52 MB |

PNG 已高度压缩，因此删除后对下载包和安装后体积的收益都很直接。

优先级：**最高**  
风险：**低**，删除前补一次全仓库及运行时引用确认即可。

### 3.2 裁剪 ONNX 的非目标平台文件

本地知识库 Embedding 功能使用 `@xenova/transformers`，引入了：

| 依赖 | 安装后体积 |
| --- | ---: |
| `onnxruntime-node` | 74 MB |
| `onnxruntime-web` | 51 MB |
| `@xenova/transformers` | 64 MB |

合计约 189 MB。

macOS ARM64 包中仍包含以下非目标平台文件：

| 文件类别 | 约占用 |
| --- | ---: |
| macOS x64 ONNX | 23 MB |
| Linux x64 ONNX | 16 MB |
| Linux ARM64 ONNX | 14 MB |
| Windows绑定 | 少量 |

当前 macOS ARM64 真正需要的是约 20 MB 的 macOS ARM64 ONNX 动态库。可以在各平台打包配置中排除其他平台目录。

预期收益：

| 指标 | 预计减少 |
| --- | ---: |
| 安装后应用 | 约 54 MB |
| DMG/ZIP | 约 18-20 MB |

优先级：**高**  
风险：**中低**，需要分别验证 macOS ARM64、macOS x64、Windows 和 Linux 构建，避免误删目标平台文件。

### 3.3 移出构建期 TypeScript/Protobuf 工具

正式包中包含：

```text
typescript                         约 28 MB
@bufbuild/protoc-gen-es
@bufbuild/protoplugin
```

项目运行时需要的是 `@bufbuild/protobuf`，而 `@bufbuild/protoc-gen-es` 属于生成 Protobuf TypeScript 代码的构建期工具。目前它位于 `dependencies`，会被 electron-builder 当作生产依赖打入应用。

建议：

1. 将 `@bufbuild/protoc-gen-es` 移到 `devDependencies`。
2. 确认生产代码没有动态调用 TypeScript 编译器。
3. 重新打包并检查 `app.asar` 中是否还存在顶层 `typescript`。

预期收益：

| 指标 | 预计减少 |
| --- | ---: |
| 安装后应用 | 约 30-60 MB |
| DMG/ZIP | 约 5-10 MB |

优先级：**高**  
风险：**低到中**，需运行构建、类型检查和 Protobuf 相关测试。

### 3.4 评估重复图像原生库

当前包中同时存在：

| 依赖 | 约占用 |
| --- | ---: |
| `@img/sharp-libvips-darwin-arm64` | 15 MB |
| `@xenova/transformers/node_modules/sharp/vendor` | 15 MB |
| `@napi-rs/canvas-darwin-arm64` | 25 MB |

`@napi-rs/canvas` 来自 `officeparser -> pdfjs-dist` 的可选依赖。两套 Sharp/Libvips 也存在重复运行时的可能。

建议先确认以下功能：

- PDF 文本和图片解析
- Word、PPT、Excel 预览与转换
- 本地知识库文档解析
- 图片缩放和格式转换

确认不需要 Canvas 或某套 Sharp 后，再通过依赖安装配置或 electron-builder 排除规则清理。

潜在收益：约 25-40 MB 安装后体积。  
优先级：**中**  
风险：**中高**，必须进行文档解析和预览回归测试。

### 3.5 评估 `onnxruntime-web`

当前本地知识库逻辑运行在主进程，主要动态导入 `@xenova/transformers`。包中同时带有约 51 MB 的 `onnxruntime-web` 和多份 WASM：

```text
ort-wasm.wasm
ort-wasm-threaded.wasm
ort-wasm-simd.wasm
ort-wasm-simd-threaded.wasm
```

需要确认 Node/Electron 主进程路径是否只使用 `onnxruntime-node`。如果确认 Web 后端不会被加载，可以排除 `onnxruntime-web` 或 Transformers 中重复的 WASM 文件。

潜在收益：

| 指标 | 预计减少 |
| --- | ---: |
| 安装后应用 | 最高约 50 MB |
| DMG/ZIP | 约 13 MB |

优先级：**中**  
风险：**中高**，需要完整验证本地知识库建库、向量化和检索。

## 4. 不建议优先处理的项目

### Electron Framework

Electron Framework 约 213 MB，是 Chromium、Node.js 和 Electron 原生运行时的主体。除非更换技术栈或升级到明显更小的 Electron 版本，否则清理空间有限。

### 内置 Node.js、Sudocode 和 Nexus

三项合计约 62 MB：

```text
Node.js   48 MB
Sudocode  8.8 MB
Nexus     5.4 MB
```

它们属于启动流程的核心运行时。移除后需要改为首次启动在线下载，会引入离线不可用、安装失败和首次启动时间增加等问题，不应作为第一批清理目标。

### `googleapis`

`googleapis` 解包后约 28 MB，但大部分是可高度压缩的 JavaScript 文本，压缩后约 1 MB。它影响安装后体积，但对 DMG 下载体积影响较小，清理优先级低于图片和原生二进制。

## 5. 推荐执行顺序

### 第一阶段：低风险直接收益

1. 删除未引用的 `public/big-paste-test.png`。
2. 将 `@bufbuild/protoc-gen-es` 移到 `devDependencies`。
3. 清理 `out/` 中旧版本 DMG、ZIP 和解包目录，避免本地磁盘误判。
4. 重新构建并记录 DMG、ZIP、`.app`、`app.asar` 的新体积。

预计 DMG 可从 373 MB 降至约 310-320 MB。

### 第二阶段：平台资源裁剪

1. macOS ARM64 包只保留 Darwin ARM64 的 ONNX 原生文件。
2. macOS x64、Windows、Linux 分别配置自己的排除规则。
3. 对每个平台执行安装启动、本地知识库建库和检索测试。

预计 DMG 可进一步下降约 18-20 MB。

### 第三阶段：可选原生依赖验证

1. 验证是否可以排除 `onnxruntime-web`。
2. 验证是否可以排除 `@napi-rs/canvas`。
3. 检查两套 Sharp/Libvips 是否可以合并或移除其中一套。

该阶段潜在收益较大，但测试成本和回归风险也最高。

## 6. 目标体积

按当前产物估算：

| 阶段 | 预计 DMG 体积 |
| --- | ---: |
| 当前 | 373 MB |
| 第一阶段完成 | 310-320 MB |
| 第二阶段完成 | 280-300 MB |
| 第三阶段完成 | 可能低于 270 MB |

以上为基于当前文件压缩率的估算，最终数字应以重新打包后的产物为准。

## 7. 每次清理后的验证

每次只处理一类资源并重新打包，至少验证：

```bash
bunx tsc --noEmit
bun run test
bun run build:mac:arm64
```

构建后记录：

```bash
ls -lh out/*.dmg out/*.zip
du -sh out/mac-arm64/*.app
du -sh out/mac-arm64/*.app/Contents/Resources/* | sort -h
```

功能回归重点：

- 应用首次启动及运行时安装
- Sudocode 对话
- Nexus 和密钥管理
- 本地知识库建库与检索
- PDF、Word、PPT、Excel 解析和预览
- 图片处理
- 终端功能
