interface:
  display_name: "ShareOne"
  short_description: "Host HTML pages and share PDF/Word/PPT"
  default_prompt: "Use $shareone to publish a local file or update an existing ShareOne link."

policy:
  allow_implicit_invocation: true
